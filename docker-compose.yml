services:
  ios-battery:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ios-battery
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
